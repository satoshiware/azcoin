# Image to run (publish this to GHCR/DockerHub/private registry)
# Example (GHCR org):
#   AZCOIN_IMAGE=ghcr.io/<org-or-user>/azcoin-node:dev
AZCOIN_IMAGE=azcoin-node:dev

# Data path inside the container (volume mount target)
AZCOIN_DATA=/data

# Network/chain selection
CHAIN=micro
MICRO=azcoin

# Ports (P2P is published; RPC is not published by default)
P2P_PORT=19333
RPC_PORT=19332

# RPC credentials (written into /data/azcoin.conf if missing)
RPC_USER=azrpc
RPC_PASSWORD=azrpcpass

# Bootstrap/peering
SEEDNODE_HOST=azcoin_node1.satoshiware.org
SEEDNODE_PORT=19333
CONNECT_TO=azcoin_node1.satoshiware.org:19333
ADDNODE=node1.azcoin.com:19333

