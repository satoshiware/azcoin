# Image to deploy (build once, run everywhere)
# Example (GHCR): ghcr.io/<org-or-user>/azcoin-node:2026-01-14
AZCOIN_IMAGE=ghcr.io/your-org/azcoin-node:latest

# Chain selection inside the container (matches contrib/docker/entrypoint.sh)
CHAIN=micro
MICRO=azcoin

# Data dir inside container
AZCOIN_DATA=/data

# Ports (container side)
P2P_PORT=19333
RPC_PORT=19332

# RPC creds (only needed if you enable RPC; keep secret in real deployment)
RPC_USER=azrpc
RPC_PASSWORD=change_me

# Bootstrap options (pick one)
# 1) Recommended: fetch peers from a known node, then connect normally:
SEEDNODE_HOST=your.seed.host.name
SEEDNODE_PORT=19333
#
# 2) Strict: connect ONLY to this node (disables auto connections)
# CONNECT_TO=1.2.3.4:19333

# 3) Manual peers to keep connected (helps when DNS seed returns 0 addresses)
# ADDNODE=azcoin_node1.satoshiware.org:19333

