# =========================
# AZCoin Node (example env)
# =========================

# Image to deploy (build once, run everywhere)
# Example (GHCR): ghcr.io/<org-or-user>/azcoin-node:2026-01-14
AZCOIN_IMAGE=ghcr.io/satoshiware/azcoin-node:latest

# Chain selection inside the container (matches contrib/docker/entrypoint.sh)
CHAIN=micro
MICRO=azcoin

# Data dir inside container
AZCOIN_DATA=/data

# Ports (container side)
P2P_PORT=19333
RPC_PORT=19332

# RPC credentials (written into /data/azcoin.conf if missing)
# Use unique values per node (do not reuse across machines).
# If left as "change_me", the entrypoint will auto-generate a unique password
# on first start and write it into /data/azcoin.conf.
RPC_USER=azrpc
RPC_PASSWORD=change_me

# =========================
# Peering / bootstrap
# =========================

# Recommended: bootstrap from a known node, then connect normally
SEEDNODE_HOST=azcoin_node1.satoshiware.org
SEEDNODE_PORT=19333

# Optional: keep some peers connected (comma or space separated)
ADDNODE=azcoin_node1.satoshiware.org:19333

# Strict mode (DO NOT enable for production unless you know why)
# CONNECT_TO=1.2.3.4:19333
